/**
 * Editorial standard for the programme block on every current and future city page.
 */
export const CITY_PROGRAM_SUBLINE = "Musicals & Shows 2026/2027";

export function getCityProgramHeading(cityName: string) {
  return `AKTUELL IN ${cityName}`;
}
